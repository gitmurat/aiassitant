{
    "name": "database_query_builder",
    "description": "Converts a human-readable query into an SQL statement to query the database, specifically using the 'afflux_transacciones_mv' table.",
    "strict": true,
    "parameters": {
      "type": "object",
      "required": [
        "humanQuery"
      ],
      "properties": {
        "humanQuery": {
          "type": "string",
          "description": "A human-readable query statement that needs to be converted into SQL"
        }
      },
      "additionalProperties": false
    }
  }